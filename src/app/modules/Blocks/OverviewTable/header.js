import styled from 'styled-components';
import { PADDING } from 'constants/styles';

const Header = styled.div`
  display: flex;
  padding: ${PADDING.NORMAL_AND_QUARTER};
  width: 100%;
`;

export default Header;
